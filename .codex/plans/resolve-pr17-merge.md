# 解决 PR17 合并冲突

## 1. 任务目标

将 `main` 合并到 PR17 分支，保留 `main` 的对比页面布局重构，其余功能以 PR17 为准，并让远端 PR 恢复可合并状态。

## 2. 当前状态

- PR17 分支为 `codex/listing-import-share-extension`。
- `main` 新增对比页滚动同步、折叠布局和相关测试。
- 三方合并发现 `project.pbxproj` 内容冲突，以及 Xcode 用户状态文件二进制冲突。
- 当前主工作区存在未提交的 Xcode 用户状态文件，不能覆盖。

## 3. 目标状态

- `ComparisonView.swift` 保留 `main` 的布局与折叠交互，同时保留 PR17 的表单提示改动。
- 其余功能文件保留 PR17 版本。
- Xcode 工程包含 PR17 的 Share Extension、导入文件和测试 Target。
- 不继续提交 Xcode 用户状态文件。

## 4. 范围边界

### 本次包括

- 在隔离工作树中合并 `origin/main`。
- 解决 Xcode 工程文件和用户状态文件冲突。
- 构建、测试、检查合并结果并推送 PR 分支。

### 本次不包括

- 不修改对比页功能设计。
- 不修改导入、图片管理和首页业务逻辑。
- 不处理主工作区已有的本地用户状态文件。

## 5. 执行里程碑

- [x] 合并 `origin/main` 并解决冲突。
- [x] 检查 `ComparisonView.swift` 与工程配置。
- [x] 构建并运行相关测试。
- [ ] 提交合并结果并推送。

## 6. 新发现与意外情况

- 发现：全量 UI 测试首次运行有 1 个横向滚动断言偶发失败，单独重跑通过。
- 影响：未改变功能代码；记录为 Simulator UI 测试稳定性风险。
- 处理方式：完成单元测试 50/50，并重跑 6 个合并相关 UI 测试，全部通过。

## 7. 决策记录

### Decision：以功能来源区分冲突处理

选择：对比页面采用 `main` 版本，其他功能采用 PR17 版本；Xcode 用户状态文件不进入合并结果。

原因：符合用户指定的合并优先级，并避免把本机界面状态作为功能变更继续传播。

备选方案：全部选择一侧文件；会丢失另一侧功能或保留无关的用户状态文件。

影响：需要重点验证工程仍包含 PR17 的 Share Extension 和测试 Target。

## 8. 验证计划

- `git diff --check`
- `xcodebuild ... build`
- 相关单元测试和对比页 UI 测试
- `git merge-tree` / `gh pr view` 确认无冲突

## 9. 风险与回滚

- Xcode 工程文件若 Target 或 Build Phase 遗漏，会导致 Share Extension 或测试无法构建。
- 若验证失败，保留当前远端 PR 分支不变，修正临时工作树后重新推送。

## 12. 结果复盘

- 已保留 `main` 的对比页滚动同步、折叠布局和对比分析区域。
- 已保留 PR17 的导入、Share Extension、图片管理、主图和表单提示功能。
- 已移除合并结果中的 Xcode 用户状态文件，并保留 `main` 的忽略规则。
- 构建通过；单元测试 50/50 通过；合并相关 UI 测试 6/6 通过。
