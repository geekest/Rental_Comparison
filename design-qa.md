# 房源页亲和视觉设计 QA

## 比较对象

- 来源视觉真值：`/tmp/codex-remote-attachments/019ffda2-436e-7643-bee9-f688585bc7f1/AF365C39-996C-4919-9068-DF5C0D12D482/1-照片-1.jpg`
- 实现截图：`/Users/geekest/Codex/2026-08-20/rental-warm-style/outputs/rental-warm-home.png`
- 合成对比：`/Users/geekest/Codex/2026-08-20/rental-warm-style/outputs/warm-style-qa-comparison.png`
- 视口与状态：iPhone 16e 纵向；来源为多抓鱼首页参考，实施为租房 App 的“正在整理 / 房源首页”状态。
- 像素与归一化：来源 `588 × 1280`，实施 `1170 × 2532`。两者均按相同内容比例裁切、归一化为 `390 × 844` 后横向对比；实施截图为 Simulator 3× 原始截图，对应 `390 × 844` pt 内容区域。

## 比较结论

该实现只借鉴参考的亲和气质，并有意保留租房决策的任务优先级；因此没有复刻参考中的搜索、分类、商品双列网格、五 Tab 导航、品牌插画或图标。

### 字体与层级

- 通过：用衬线展示字强调“上海租房计划”和房源名，正文保持系统无衬线以保障中文可读性；墨色标题与灰褐辅助信息形成清晰层级。
- 通过：卡片内长标题限制为两行以内，当前“下一步”描述在窄宽下换行但不裁切。

### 间距与布局节奏

- 通过：20 pt 水平边距、26 pt 主区块间距、24 pt 圆角和低对比描边形成轻量分组；没有默认浮层阴影或卡片套卡片。
- 通过：顶部任务区、下一步、候选区依序呈现；持久 Tab 栏不遮挡顶部内容，候选卡片可横向分页。

### 色彩与视觉 token

- 通过：米白画布、纸张白表面、墨色文字、苔绿操作、杏黄行动区和橙褐风险提示分别映射基础、主操作和待确认语义。
- 通过：主操作与风险色没有混用，正文与背景保持可读对比。

### 图片与图标

- 通过：继续使用项目内真实房源照片并固定为 16:9 fill 裁切；未用占位图替代。
- 通过：使用 SF Symbols 作为系统级图标，图标线宽、圆形操作区和 Tab 选中态保持一致；未复制参考产品的猫咪插画或品牌资产。

### 文案与交互

- 通过：保留“加入/移除对比”、详情跳转、快速添加、3 个 Tab 和待确认数量等原有交互及无障碍 identifier。
- 通过：完整 Simulator 回归显示 36 项单元测试和 4 项 UI 测试均通过。

## 比较历史

- [P2，已修复] 首次渲染中“待选房源”数量显示成 `\(candidates.count)` 字面量。
  - 修复：在 `Rental_Comparison/ListingsView.swift` 恢复 Swift 字符串插值。
  - 修复后证据：最终 Simulator 截图显示“3 套”。

## Focused region comparison

无需额外局部截图：顶部任务区、下一步卡片、候选标题、图片裁切、状态标签与底部导航均在合成全图中清晰可辨；源图和实现图的功能不同，逐像素的图标比对不适用。

## Findings

无可操作的 P0、P1 或 P2 视觉差异。来源截图中的商城搜索/分类/商品网格是参考产品信息架构，不属于租房决策首页的预期功能。

## Implementation Checklist

- [x] 保留原有任务、候选、对比、详情和快速添加的交互。
- [x] 应用亲和视觉 token 到首页与 3 Tab 导航。
- [x] 验证 16:9 房源图片裁切与长文案基础换行。
- [x] 完整 Simulator 自动化回归。

## Follow-up Polish

- [P3] 若后续扩展全 App 风格，可把相同 token 逐步应用到比较、待确认、详情与编辑页；本次不改变这些页面的信息架构。

final result: passed
