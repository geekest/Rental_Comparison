# Rental_Comparison 项目上下文

## 当前阶段

产品方向、Web 验证 MVP 边界和核心流程已经确认，尚未进入正式实现。

当前仓库曾生成并暂存一套 Xcode 模板工程，但相关工程文件随后从工作区删除。该现场不是本轮文档工作产生的，在确认处理方式前不得恢复、删除或继续扩展。当前下一实施阶段按产品共识应是手机 Web 验证原型，而不是直接开发 iPhone App。

## 已确认事项

### 产品方向

- 产品帮助租房者看清事实并自主完成选房决策。
- 首轮验证中国大陆城市长租用户，通过小红书招募。
- 首版同时允许整租住宅和合租单间进入同一候选池。
- 正式第一版目标平台为 iPhone，Web 只用于验证。

### 产品边界

- 产品没有业务服务器，房源内容保存在用户当前设备。
- Web 原型使用本地 OCR 与手动确认，不接远程生成式 AI。
- 不提供综合评分、自动推荐、账户、云同步或多人协作。
- 接受低频、任务完成型产品属性。

### 核心对象

- **选房任务**：一次具有明确城市和决策目标的租房选择过程。
- **租赁方案**：参与比较的对象，可以是整套住宅或合租中的独立卧室。
- **费用明细**：具有金额、货币、周期、可退和确认属性的费用记录。
- **条件**：具有重要程度，并能针对每套方案记录符合、不符合或未知的决策标准。
- **看房检查项**：记录未检查、未发现问题或发现问题的现场证据项。
- **决策变化记录**：保存在本地的重点考虑、淘汰、恢复、最终确认和撤回记录。

### 状态与长期不变量

- 租赁方案状态只有候选和已淘汰；已淘汰可以恢复。
- 重点考虑是可多选标记，不属于状态。
- 基准房源只用于比较，不代表系统推荐。
- 每个选房任务最多拥有一套最终房源。
- 最终房源可以撤回到候选池；撤回后任务重新进入进行中状态并允许继续比较。
- 未知费用不使用 `0` 替代。
- 可退押金属于首期现金压力，但不属于月均居住成本。
- 通勤支出与住房成本分开显示。
- 整租面积和合租私人空间面积不直接判断优劣。

## 文档入口

- [项目说明](README.md)
- [产品简述](docs/product/product_brief.md)
- [MVP 范围](docs/product/mvp_scope.md)
- [核心流程规格](docs/specs/spec_001_core_flow.md)
- [本地存储策略 ADR](docs/adr/0001_local_storage_strategy.md)
- [设计草图索引](docs/design/sketch_map.md)

## 开发前准备度检查

### 阻塞下一行产品代码

1. **确定下一实现目标和目录**：当前产品顺序要求先做 Web 原型，但仓库目录和暂存区曾按 iPhone 工程初始化。必须决定 Web 原型放在本仓库的新目录、替代当前骨架，还是使用独立仓库。
2. **处理现有 Git 现场**：Xcode 模板文件已暂存但从工作区删除，且暂存内容包含 `xcuserdata`。在继续实现前，需要由用户决定保留、恢复或移除；不得由 Agent 猜测。
3. **确定 Web 技术栈**：尚未确认语言、框架、包管理器、开发服务器、构建工具和部署方式，因此不存在可验证的安装、启动或构建命令。
4. **确定支持环境**：需要明确首轮支持的手机浏览器版本、是否允许添加到主屏幕、离线要求，以及浏览器存储不可用时的降级行为。
5. **验证本地 OCR 可行性**：需要用真实中国大陆房源截图验证中文、数字和费用字段识别；若不可行，P0 必须安全退化为截图对照与手动录入。
6. **确定本地持久化方案**：需要决定数据存储、图片保存、数据升级、容量边界和清除后的提示行为，并更新 ADR-0001。
7. **确定原型信息架构和关键状态稿**：至少需要空状态、候选列表、添加方案、费用编辑、横向比较、看房记录、最终确认、撤回最终房源和本地数据丢失提示。
8. **定义验证和检查命令**：工程建立后需要补充单元测试、端到端测试、lint、typecheck 和 format 命令，并将已验证命令更新到 `AGENTS.md` 和 `README.md`。

### 进入 iPhone TestFlight 前阻塞

1. 最低 iOS 版本和仅支持 iPhone 的 Target 设置；
2. App 标识、正式名称、签名团队和 TestFlight 分发配置；
3. SwiftUI 工程结构、状态管理与本地持久化框架；
4. 本地图片、OCR、导出和数据迁移方案；
5. 隐私说明、数据导出与删除行为；
6. 单元测试和 UI 测试的覆盖边界；
7. Web 验证指标达到进入 TestFlight 的门槛。

### 可以延后确认

- 30～60 天单次通行证和永久买断的具体价格；
- 远程生成式 AI 供应商及安全接入方式；
- 多人协作和免安装评分；
- 香港、台湾及其他地区模板；
- Android、iPad 和正式 Web 产品；
- 租后 App 数据移交。

## 当前未确认的工程信息

- Web 原型的技术栈、目录与部署方式；
- 支持浏览器和离线范围；
- 本地 OCR 方案；
- 本地存储、备份和数据升级策略；
- 设计系统、视觉规范和页面稿；
- 原生 iPhone 阶段的最低系统版本与工程架构。

未知工程信息不得在 `AGENTS.md`、README 或 ADR 中被写成已经验证的事实。

## 更新要求

- 产品目标、用户问题和指标更新到 `docs/product/`。
- 单个功能的完整可观察行为更新到 `docs/specs/`。
- 跨功能术语、状态和长期不变量更新到本文件。
- 难以逆转的存储、平台和架构选择更新到 `docs/adr/`。
- 临时执行步骤和短期进度不写入长期文档。
