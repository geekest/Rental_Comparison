# Rental_Comparison 项目上下文

## 当前阶段

原生 iOS 是当前主开发平台，当前开发基线为 `codex/listing-import-share-extension`。产品已进入 **Rental Decision System v2** 的持续迭代：帮助用户捕获候选、确认事实与证据、解决高影响未知项，并完成可解释的租房决策。`web_version/` 已冻结为历史验证版本，不继续同步 v2 功能。

v2 的权威行为见 [Decision Readiness 重构规格](docs/specs/spec_002_decision_readiness_rebuild.md)，数据迁移与本地文件兼容策略见 [ADR-0003](docs/adr/0003_decision_model_v2_migration.md)。

原生 iOS 使用版本化 Codable JSON 保存决策状态，媒体保存于应用本地文件；v1 状态可迁移到 v2，迁移失败时保留旧状态并回退读取。产品没有业务服务器、账户、云同步或自动埋点。iOS 本地 OCR 和链接解析只产生待确认建议，失败时保留原始素材并降级为手动填写。Web 原型仍使用 IndexedDB 和浏览器本地 OCR，但仅用于历史验证。

核心决策闭环已经实现并有自动化验证；完整 PRD 中尚未实现的字段、比较分组和真机验证以[核心流程规格的实现状态](docs/specs/spec_001_core_flow.md#当前-web-实现状态)为准。不得把“核心流程通过”解释为全部字段已经完成。

## 已确认事项

### 产品方向

- 产品帮助租房者看清事实、证据、关键差异和未知项，并自主完成选房决策。
- 首轮验证中国大陆城市长租用户，通过小红书招募。
- 首版同时允许整租住宅和合租单间进入同一候选池。
- 正式第一版目标平台为 iPhone，Web 只用于验证。

### 产品边界

- 产品没有业务服务器，房源内容保存在用户当前设备。
- Web 原型使用本地 OCR 与手动确认，不接远程生成式 AI。
- 不提供综合评分、自动推荐、账户、云同步或多人协作。
- 接受低频、任务完成型产品属性。

### v2 核心对象

- **Hunt**：一次选房决策任务；首版仍只允许一个 active Hunt。
- **Option**：一个真实候选租赁方案；现实进度 `SearchStage` 与用户判断 `DecisionState` 分开记录。
- **Fact**：关于某个 Option 的结构化已知信息，带来源与确认状态。
- **Evidence**：事实或判断的原始证据元数据；证据不自动等于真相。
- **Unknown**：缺失且可能影响当前决策的信息，不是所有空字段的集合。
- **VerificationTask**：为解决 Unknown 而执行的询问、检查、观察、拍照或测量行动。
- **Criterion**：用户关心的硬性条件、偏好或忽略项。
- **DecisionEvent**：真正改变决策上下文的行为记录。

### v1 兼容对象

- **选房任务**：一次具有明确城市和决策目标的租房选择过程。
- **租赁方案**：参与比较的对象，可以是整套住宅或合租中的独立卧室。
- **费用明细**：具有金额、房源货币、每日/每月/每季度/每半年/每年/一次性周期、可退和确认属性的费用记录。
- **条件**：具有重要程度，并能针对每套方案记录符合、不符合或未知的决策标准。
- **看房检查项**：记录未检查、无问题或有问题的现场证据项；只有有问题时才可选填记录和照片。
- **决策变化记录**：保存在本地的重点考虑、淘汰、恢复、最终确认和撤回记录。

### 状态与长期不变量

- 租赁方案状态只有候选和已淘汰；已淘汰可以恢复。
- 重点考虑是可多选标记，不属于状态。
- 基准房源只用于比较，不代表系统推荐。
- 每个选房任务最多拥有一套最终房源。
- 最终房源可以撤回到候选池；撤回后任务重新进入进行中状态并允许继续比较。
- 未知费用不使用 `0` 替代。
- 可退押金属于首期现金压力，但不属于月均居住成本。
- 货币允许用户自由填写；不同货币默认不可直接比较，也不自动换算。
- 通勤支出与住房成本分开显示。
- 整租面积和合租私人空间面积不直接判断优劣。
- Unknown 不等于 `0`，也不等于硬性冲突。
- 用户确认、现场观察和未确认提取的信息必须可区分；Evidence 不自动决定事实真伪。
- 默认对比按差异、硬性冲突和 blocker 组织；全量矩阵是二级能力。
- 最终确认必须呈现 unresolved blocker、已知风险、关键取舍和证据覆盖情况。
- Universal Core 不得硬编码 CNY、`zh_CN`、平方米或中国大陆特有字段；地区差异由 Regional Template 承担。
- 不同货币不得静默比较或自动换汇。

## 文档入口

- [项目说明](README.md)
- [产品简述](docs/product/product_brief.md)
- [MVP 范围](docs/product/mvp_scope.md)
- [核心流程规格](docs/specs/spec_001_core_flow.md)
- [本地存储策略 ADR](docs/adr/0001_local_storage_strategy.md)
- [设计草图索引](docs/design/sketch_map.md)
- [Web 原型页面映射](docs/design/web_prototype_map.md)
- [Web 工程说明](web_version/README.md)
- [开发状态与分支进度](docs/development_status.md)

## 开发前准备度检查

### 进入真实用户验证前仍需准备

1. 用至少 3 类真实中国大陆房源截图记录 OCR 的字段命中率、耗时和失败原因；手动降级已实现。
2. 在真实 iPhone Safari 上验证图片选择、中文输入、页面滑动、导出文件和 IndexedDB 保留行为。
3. 准备统一的用户日记、回访提纲和问题反馈接收方式；原型不自动埋点。
4. 明确测试参与者的数据清理说明，以及浏览器被清理后不可恢复的风险提示。
5. 从核心流程规格的剩余差距中确定首轮测试必须补齐的字段，避免测试用户因缺少租约、来源或通勤信息而无法完成真实比较。

### 进入 iPhone 真机 / TestFlight 前阻塞

1. 真机系统分享面板、相册授权、中文输入和导入确认的逐项验收；
2. 链家、贝壳、Reddit 在线页面变化、反爬和失败降级的持续验证；
3. App 标识、正式名称、签名团队和 TestFlight 分发配置；
4. 隐私说明、数据导出与删除行为；
5. 单元测试和 UI 测试的覆盖边界；
6. 迁移、媒体文件和导入失败的恢复体验。

### 可以延后确认

- 30～60 天单次通行证和永久买断的具体价格；
- 远程生成式 AI 供应商及安全接入方式；
- 多人协作和免安装评分；
- 香港、台湾及其他地区模板；
- Android、iPad 和正式 Web 产品；
- 租后 App 数据移交。

## 当前未确认的工程信息

- Web 原型最终用于测试的静态托管平台和访问域名；
- 真实 iPhone Safari 的最低支持版本与存储保留表现；
- 本地 OCR 在不同平台房源截图上的真实识别质量；
- 真实 iPhone 首轮验收的设备范围与发布节奏。

未知工程信息不得在 `AGENTS.md`、README 或 ADR 中被写成已经验证的事实。

## 更新要求

- 产品目标、用户问题和指标更新到 `docs/product/`。
- 单个功能的完整可观察行为更新到 `docs/specs/`。
- 跨功能术语、状态和长期不变量更新到本文件。
- 难以逆转的存储、平台和架构选择更新到 `docs/adr/`。
- 临时执行步骤和短期进度不写入长期文档。
