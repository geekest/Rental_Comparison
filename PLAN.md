# 租房对比开发看板

> 最后盘点：2026-08-13
> 当前基线：`34b7eb9`（`main`）
> 用途：这是产品开发看板，不替代 `docs/specs/` 中已确认的功能规格。每次开始或完成一个需求时，更新对应任务的状态、验收证据和关联文档。

## 使用约定

- `- [ ]` 表示待办，`- [x]` 表示已完成；进行中的事项在标题后标注“进行中”。
- 新需求先写入“待确认”，确认范围和验收标准后再移入“待办”。
- 涉及数据结构、IndexedDB 兼容或产品边界的改动，先更新相应规格或 ADR，再开始实现。
- 每个已完成的开发任务应补充：改动文件、自动化验证和手动验证证据。

## 当前项目结构

| 目录或文件 | 职责 |
| --- | --- |
| `docs/product/` | 产品目标与 MVP 范围 |
| `docs/specs/spec_001_core_flow.md` | 已确认的核心流程、实现状态和验收标准 |
| `docs/adr/` | 不易回滚的技术决策 |
| `CONTEXT.md` | 业务术语、状态和长期不变量 |
| `web_version/src/Prototype.tsx` | 手机 Web 原型的主界面与交互编排 |
| `web_version/src/domain.ts` | 任务、房源、费用、条件、看房和决策事件模型 |
| `web_version/src/calculations.ts` | 成本、硬性冲突和看房异常计算 |
| `web_version/src/storage.ts` | IndexedDB 状态与本地媒体保存 |
| `web_version/src/ocr.ts` | 本地 OCR 建议生成 |
| `web_version/src/report.ts` | 决策报告和匿名测试摘要导出 |
| `web_version/tests/` 与 `web_version/src/*.test.ts` | E2E、运行时和单元测试 |

## 已完成

- [x] 明确产品边界：帮助用户基于事实、差异、未知项和风险自主决策；不提供综合评分或自动推荐。
- [x] 建立手机 Web MVP：React + TypeScript + Vite，所有业务数据仅保存在当前浏览器的 IndexedDB。
- [x] 支持创建单一选房任务，以及用 5 个必填字段建立候选房源。
- [x] 支持本地截图和本地 OCR；识别结果需经用户确认才会写入房源数据。
- [x] 支持 2–5 套候选房源比较、基准房源左置、成本/通勤/条件/看房四组对比。
- [x] 支持费用拆分：首期现金、月均居住成本、未知费用提示，以及可退押金不摊入月均成本。
- [x] 支持条件重要性、看房清单与异常照片、重点考虑、淘汰/恢复、唯一最终房源及撤回选择。
- [x] 支持不含原始图片的决策报告和匿名测试摘要导出。
- [x] 已具备单元测试、Playwright 核心流程测试、移动运行时检查、构建和静态托管检查。
- [x] 修复原生 iOS 房源卡片交互与布局（2026-08-17）
  - 改动：已加入对比可再次点击撤销；点击卡片非操作区进入详情；图片固定在 16:9 容器内以 fill 裁切；楼层统一展示为“X 楼”。
  - 文件：`Rental_Comparison/ListingsView.swift`、`Rental_Comparison/ListingImageView.swift`、`Rental_ComparisonUITests/RentalComparisonUITests.swift`。
  - 验证：iPhone 16e 模拟器执行 7 个单元测试和 3 个 UI 测试均通过；以用户新增的 4288×2848 照片检查固定裁切、楼层和操作按钮。

## 待确认

- [ ]

## 待办

### 产品功能
#### 1.房源展示页
- [ ] 房源首页卡片信息，需展示和记录楼层
- [ ] 如同一套房源保存了多张照片，允许用户在房源卡片单卡片能横向滑动照片。涉及到其他位置滑动时再整体滑动卡片。
- [ ] 如果是合租，显示为「合租 X 居」，X 为用户自定义输入。
- [ ] 如果是整租，显示为「整租 X 居」，X 为用户自定义输入。
- [ ] 通勤信息内，显示通勤方式，例如（地铁、步行、开车）

#### 2.添加房源
- [ ] 支持调用定位能力，在地图上搜索位置或者选点确认房子位置。
- [ ] 截图识别应将能识别的内容信息，和支持录入的内容，进行匹配，不再生硬的强行识别，现在的识别效果较差，需要能够将可识别的内容，都架构化的填充到信息中。
- [ ] 添加房源的首页支持填写完主要信息后，新增下拉展开栏支持填写剩余补充信息，而不是两个跳转的页面；添加房源填写完主要信息后的下拉展开栏里的额外信息页面，按照用户的需要填写，页面最下方展示保存按钮。
- [ ] 补充信息栏里的地址，支持定位和调用地图选点能力。
- [ ] 货币不再限制货币单位。
- [ ] “真实成本”文本替换为“其他费用”；栏目包含两个子板块：押金和其他费用
  - [ ] 预付租金的输入栏太宽了，只需要填写数字即可；不要用预付租金，要用押金来指代。押金默认可退。
  - [ ] 其他费用里，每一条明细支持用户定义 3 个关键信息，费用名称、金额、周期费用（每日/每月/每季度/每半年/每年或者一次性费用）
- [ ] 不再展示「条件结果」板块
- [ ] 「异常优先看房清单」调整文本为「看房清单」
  - [ ] 检查的枚举值统一调整为（未检查、无问题、有问题）
  - [ ] 只有有问题才需要记录，但记录和照片不是必须要求

#### 3.房源对比
- [ ] 不要用 4 个 tab 区分对比，用一个 长页面展示所有的对比。几个 tab 只是为了快速定位到长页面的模块位置。
- [ ] 所有的对比项目都要对齐苹果的产品对比展示交互和显示方式。
- [ ] 适当的位置可以使用 SF symbol 来加强显示和效果，但不要到处滥用。

### 真实用户验证与平台验收

- [ ] 在真实 iPhone Safari 上完成首轮验收：图片选择、中文输入、OCR、导出、刷新后 IndexedDB 保留、候选到最终选择闭环。
  - 验收：记录设备、iOS 版本、浏览器版本和每项结果；Playwright WebKit 不可替代此项。
- [ ] 招募并完成至少 10 名有效用户测试，收集用户主动导出的匿名测试摘要和访谈/日记证据。
  - 验收：对照 `docs/product/mvp_scope.md` 的 4 项通过门槛，输出是否进入原生 iPhone 阶段的结论。

### 渐进录入与数据模型

- [ ] 补齐已确认但尚无编辑入口的房源字段：来源链接、楼层、电梯、可入住日期、租期和付款周期。
  - 前置：先确认字段分组和首次录入不增加负担的交互方案。
  - 验收：未知值保持显式；新增字段可保存、刷新恢复，并在需要时进入比较或计算。
- [ ] 支持单笔费用的货币与更细付款周期，明确不同货币不可直接比较时的展示规则。
  - 前置：更新规格；如需改变既有本地数据结构，记录兼容方案。
- [ ] 补齐通勤出行方式、每月通勤天数和月通勤支出估算，并继续与住房成本分开显示。
  - 验收：费用公式、未知项和比较结果均有单元测试。

### 比较与决策证据

- [ ] 将位置、空间和租约条件从现有比较页拆成独立分组。
  - 验收：整租面积与合租私人空间仍不被自动判定优劣。
- [ ] 在最终确认前汇总所有未检查的看房项，并与已记录异常、硬性冲突和未知费用明确区分。
  - 验收：最终确认页和导出的决策报告均可追溯这些风险。

### 稳定性与工程质量

- [ ] 独立复现并定位完整 `npm run test:runtime` 中仍失败的 2 个 Chrome 专用移动手势用例。
  - 验收：仅在明确根因后修改受保护运行时文件；不得通过弱化完整性检查绕过失败。
- [ ] 为 IndexedDB 制定版本迁移和完整本地备份格式。
  - 前置：先形成 ADR，并定义旧数据、媒体文件和导入失败的处理方式。

## 已知风险与限制

- Web MVP 仅用于验证，不承诺成为正式 Web 产品；正式第一版目标仍是 iPhone。
- 当前仅验证中国大陆模板、`CNY` 和平方米；跨地区、跨货币不在当前可用范围内。
- 本地浏览器数据被清理或更换设备后可能丢失；目前没有账户、云同步或业务服务器恢复能力。
- 真实 iPhone Safari 尚未完成验收；已有浏览器自动化通过不等于真机通过。
- 完整移动运行时套件仍有 2 个 Chrome 专用手势用例失败，不能标记为全绿。

## 本轮盘点记录

- [x] 阅读产品、范围、核心流程、上下文、Web 原型代码、测试与运行说明。
- [x] 确认当前主线基线为 `34b7eb9`，盘点开始时工作树无未提交改动。
- [x] 将“已实现”与“规格中尚未完整实现/待真机验证”分离记录。
- [x] 创建本看板；本轮未修改业务代码、产品规格或依赖。

## 后续更新模板

```md
### <需求名称>

- 状态：待办 / 进行中 / 已完成 / 阻塞
- 目标：
- 范围：
- 验收：
- 改动文件：
- 验证证据：
- 风险或后续：
```
